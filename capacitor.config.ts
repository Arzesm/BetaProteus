import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.vibrantpangolinswoop',
  appName: 'vibrant-pangolin-swoop',
  webDir: 'dist'
};

export default config;
