# Интеграция системы расчета лунных фаз с SwissEph для Москвы

## Что было сделано

### 1. Интеграция SwissEph для точных астрономических расчетов для Москвы
- **Добавлено**: Использование SwissEph для расчета лунных фаз с учетом координат Москвы
- **Преимущества**: Точные астрономические данные для Москвы, консистентность с натальной картой
- **Fallback**: Упрощенный расчет при недоступности SwissEph

### 2. Настройка для Москвы
- ✅ **Координаты Москвы**: Широта 55.7558°, долгота 37.6176°
- ✅ **Часовой пояс**: UTC+3 (московское время)
- ✅ **Конвертация времени**: Автоматическая конвертация московского времени в UTC
- ✅ **Точные расчеты**: Учет географического положения Москвы

### 3. Система кэширования и оптимизации
- ✅ **Кэширование данных**: 24-часовой кэш для уменьшения повторных расчетов
- ✅ **Умная обработка ошибок**: Автоматическое переключение на fallback при ошибках SwissEph
- ✅ **Статистика кэша**: Мониторинг использования кэша
- ✅ **Очистка кэша**: Функции для управления кэшем

### 4. Обновлены компоненты

#### `src/lib/moonCalculations.ts`
- ✅ Добавлена функция `calculateMoonPhaseWithSwissEph()` - точный расчет с SwissEph для Москвы
- ✅ Добавлена функция `convertToUTC()` - конвертация московского времени в UTC
- ✅ Обновлена основная функция `getMoonData()` - приоритет SwissEph для Москвы
- ✅ Добавлено кэширование с автоматическим управлением
- ✅ Улучшена обработка ошибок SwissEph

#### `src/components/dreams/DreamJournalForm.tsx`
- ✅ Обновлен для использования SwissEph для Москвы
- ✅ Добавлена обработка ошибок с fallback
- ✅ Асинхронное получение данных о луне



### 5. SwissEph интеграция для Москвы

#### Конфигурация Москвы
```typescript
const MOSCOW_COORDS = {
  latitude: 55.7558,  // Широта Москвы
  longitude: 37.6176, // Долгота Москвы
  timezone: 3         // Часовой пояс Москвы (UTC+3)
};
```

#### Конвертация времени
```typescript
// Конвертация московского времени в UTC
function convertToUTC(date: string, time?: string) {
  // Обработка часового пояса Москвы
  const utcHour = hour - MOSCOW_COORDS.timezone;
  // Обработка перехода через полночь
}
```

#### Расчет лунной фазы
```typescript
// Получение позиций Луны и Солнца для Москвы
const moonPosition = swe.calc_ut(julianDay, swe.SE_MOON, swe.SEFLG_SWIEPH);
const sunPosition = swe.calc_ut(julianDay, swe.SE_SUN, swe.SEFLG_SWIEPH);

// Расчет элонгации (разности долгот)
let elongation = moonLongitude - sunLongitude;
if (elongation < 0) elongation += 360;
```

#### Определение фазы на основе элонгации
- **0-45°**: Новолуние 🌑 (0-15% освещенности)
- **45-90°**: Растущий серп 🌒 (15-50% освещенности)
- **90-135°**: Первая четверть 🌓 (50-85% освещенности)
- **135-180°**: Растущая луна 🌔 (85-100% освещенности)
- **180-225°**: Полнолуние 🌕 (100-85% освещенности)
- **225-270°**: Убывающая луна 🌖 (85-50% освещенности)
- **270-315°**: Последняя четверть 🌗 (50-15% освещенности)
- **315-360°**: Убывающий серп 🌘 (15-0% освещенности)

#### Точный расчет освещенности
```typescript
// Формула точной освещенности Луны
const illumination = Math.round(((1 + Math.cos((180 - elongation) * Math.PI / 180)) / 2) * 100);
```
Эта формула дает точное значение освещенности от 0% до 100% на основе угла элонгации между Луной и Солнцем:
- **Элонгация 0°** (новолуние) = **0%** освещенности
- **Элонгация 90°** (первая/последняя четверть) = **50%** освещенности  
- **Элонгация 180°** (полнолуние) = **100%** освещенности

### 6. Система кэширования

```typescript
// Кэш данных о луне для Москвы
const moonDataCache = new Map<string, { data: MoonData; timestamp: number }>();
const CACHE_DURATION = 24 * 60 * 60 * 1000; // 24 часа

// Приоритет получения данных:
1. Кэш (если данные актуальны)
2. SwissEph (точный астрономический расчет для Москвы)
3. Упрощенный расчет (fallback)
```

### 7. Обработка ошибок SwissEph

```typescript
// Умная обработка ошибок SwissEph
try {
  const moonData = await calculateMoonPhaseWithSwissEph(date, time);
  return moonData;
} catch (error) {
  console.error('Ошибка расчета с SwissEph:', error);
  // Fallback на упрощенный расчет
  return calculateMoonPhaseFallback(date);
}
```

### 8. Преимущества новой системы

- ✅ **Точность**: Профессиональные астрономические расчеты SwissEph для Москвы
- ✅ **Географическая точность**: Учет координат и часового пояса Москвы
- ✅ **Точная освещенность**: Реальные значения от 0% до 100% на основе астрономических формул
- ✅ **Консистентность**: Одинаковая точность с натальной картой
- ✅ **Надежность**: Множественные fallback'и
- ✅ **Скорость**: Кэширование и оптимизация
- ✅ **Актуальность**: Точные данные для любой даты в Москве
- ✅ **Устойчивость**: Автоматическое переключение при ошибках
- ✅ **Совместимость**: Работает с существующим кодом

### 9. Отображение в интерфейсе

При записи сна теперь отображается (для Москвы):
- 🌑 Фаза луны с эмодзи (из SwissEph или fallback)
- ♈ Знак зодиака с символом (из SwissEph или fallback)
- 75% Процент освещенности (из SwissEph или fallback)
- Красивая карточка с градиентным фоном

### 10. Управление кэшем

```typescript
// Очистка кэша
import { clearMoonDataCache } from '@/lib/moonCalculations';
clearMoonDataCache();

// Получение статистики кэша
import { getCacheStats } from '@/lib/moonCalculations';
const stats = getCacheStats();
console.log('Размер кэша:', stats.size);
console.log('Записи в кэше:', stats.entries);
```

### 11. Обработка ошибок

Система обрабатывает:
- Ошибки инициализации SwissEph
- Ошибки расчета позиций планет
- Ошибки конвертации дат и времени
- Ошибки часового пояса
- Автоматический fallback на упрощенный расчет

## Использование

### Для разработчиков
```typescript
import { getMoonData, clearMoonDataCache, getCacheStats } from '@/lib/moonCalculations';

// Получение данных о луне для Москвы
const moonData = await getMoonData('2025-08-07');
console.log(moonData.phase, moonData.sign, moonData.illumination);

// Управление кэшем
clearMoonDataCache();
const stats = getCacheStats();
```

### Для пользователей
1. Перейдите в раздел "Сны"
2. Нажмите "Записать новый сон"
3. Выберите дату сна
4. Автоматически загрузится информация о луне для Москвы
5. При ошибке SwissEph автоматически используется упрощенный расчет

## Конфигурация

### SwissEph настройки для Москвы
- **Инициализация**: Автоматическая при первом использовании
- **Флаги**: SEFLG_SWIEPH для точных расчетов
- **Планеты**: SE_MOON (Луна) и SE_SUN (Солнце)
- **Координаты**: Москва (55.7558°N, 37.6176°E)
- **Часовой пояс**: UTC+3 (московское время)

### Лимиты и кэширование
- **Кэш**: 24 часа для каждой даты
- **Fallback**: Упрощенный расчет при ошибках SwissEph
- **Производительность**: Кэширование уменьшает повторные расчеты
- **Точность времени**: Учет московского часового пояса

## Мониторинг

В консоли браузера отображается:
- ✅ Успешные расчеты с SwissEph для Москвы
- ⚠️ Ошибки SwissEph с деталями
- 🔄 Fallback на упрощенный расчет
- 💾 Использование кэша
- 📊 Время выполнения расчетов
- 🚫 Обработка ошибок SwissEph
- 🌍 Информация о конвертации времени Москва → UTC
- 🌙 Точная освещенность Луны в процентах

## Производительность

### Оптимизации:
- **Кэширование**: Уменьшает количество повторных расчетов
- **SwissEph**: Профессиональные астрономические алгоритмы
- **Fallback**: Быстрая работа при проблемах с SwissEph
- **Автоматическое восстановление**: Повторные попытки SwissEph
- **Географическая оптимизация**: Расчеты специально для Москвы

### Метрики:
- **Скорость**: Мгновенная загрузка из кэша
- **Надежность**: 100% доступность благодаря fallback
- **Точность**: Профессиональные астрономические данные SwissEph для Москвы
- **Консистентность**: Одинаковая точность с натальной картой
- **Географическая точность**: Учет координат и времени Москвы

Это обеспечивает максимальную точность данных о лунных фазах для Москвы при сохранении надежности и производительности системы, используя ту же технологию, что и для расчета натальной карты.
